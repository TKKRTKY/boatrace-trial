# 事前登録

結果を見る前に、何をどの基準で判断するかをここに固定します。
結果を見た後にここを書き換えた場合、その分析は「探索的」と明記し、判断には使いません。

登録日時: 2026-09-17（直前情報の比較を実行する前）

---

## 1. 直前情報の効果の比較

### 問い

直前情報（グループ `beforeinfo`：展示進入、進入の変化量、展示ST、その場内偏差と順位、展示フライング、チルト、部品交換数、プロペラ交換、調整重量、体重変化、気温・水温）を加えると、1着確率の予測は良くなるか。

### 実行方法（固定）

```bash
.venv/bin/boat-agent build    --source official --data-root ./data-official
.venv/bin/boat-agent features --as-of 2026-01-01 --from 2023-01-01 --to 2025-12-31 --data-root ./data-official
.venv/bin/boat-agent ablate   --data-root ./data-official
```

- 学習 2025-01-01〜2025-04-20、較正 2025-04-27〜2025-06-01、評価 2025-06-08〜2025-12-31（`ablate` の既定値）
- LightGBM のハイパーパラメータは既定値のまま（葉15、葉あたり最小1,000行、学習率0.03、L2=20）。この比較のために変えない。
- シードは 1, 2, 3。各群で3つのモデルの予測確率を平均し、その平均で比べる。

### 主指標

Δ = 評価期間の log loss（直前情報あり）− log loss（直前情報なし）

レース単位の差の平均として計算し、95%区間を付ける。負なら直前情報が効いている。

### 比較が有効である条件

Δを読む前に確認する。1つでも満たさなければ判断しない。原因を直して再実行し、その経緯を記録する。

1. 2025年のレースのうち、直前情報が付いている割合が **98%以上**
2. 学習期間のレース数が **15,000以上**
3. 両群とも ECE が **0.01以下**

### 判定

| 結果 | 判定 | 次の行動 |
|---|---|---|
| Δ ≤ −0.003 かつ 95%区間の上限 < 0 | **採用** | 直前情報2023〜24年の取得を続け、3年分のモデルに組み込む |
| −0.003 < Δ かつ 95%区間の上限 < 0 | **弱い効果** | 取得は続ける。採否は §2 の封印データで決める |
| 95%区間が0を含む、または Δ ≥ 0 | **効果なし** | 直前情報2023〜24年の取得を予定から外す。締切前の直前情報の取得は続ける |

基準値 0.003 は、3連単市場とモデルの差（0.036）の約1割にあたる。

### 参考として報告するもの（判断には使わない）

- 各群の、3連単市場との差（全体、および進入順が変わったレースだけ）
- 各群の、市場との混合による上積み
- シードごとの Δ

### やってはいけないこと

結果を見た後に、期間・ハイパーパラメータ・特徴量・基準値を変えて再実行し、その結果で判断すること。

---

## 2. 封印データ（最終確認用）

### 範囲

**2026-01-01〜2026-08-31 のレース**（38,064レース）

### 封印中に禁止すること

- この期間の成績（log loss 等）を計算する・見ること
- この期間の結果が表示される再現（`replay` / `analyze`）
- この期間を見て、特徴量・ハイパーパラメータ・判定基準を決めること

### 封印中に許すこと

- データの取得と正規化
- 形式の検証（件数、欠損、パースの可否）

### 最終確認（一度だけ）

1. 最終モデルの設定をコミットしてから封印を解く。
2. 最終モデルの期間：
   - 学習 2023-01-01〜2025-09-30
   - 較正 2025-10-08〜2025-12-24
   - 評価 = 封印データ
3. 報告する数字：
   - 艇番のみのモデルとの比較
   - 3連単市場との差（偏り補正と混合の重みは較正期間で決める）
   - 市場との混合による上積み
   - §1 で「採用」または「弱い効果」になった場合は、直前情報あり／なしの差
4. 封印を解いた日時・理由・コミットを JOURNAL に記録する。

コード側でも守る：評価に封印期間が含まれる場合は、環境変数 `BOAT_AGENT_UNSEAL_HOLDOUT` に理由を設定しない限りエラーにし、解除は `logs/holdout_unsealed.log` に記録する。

必要なデータ：2026年1〜8月の直前情報と3連単（取得予定に追加済み）。

---

## 3. ハイパーパラメータの選び方

- 候補の比較は、**学習期間の末尾15%（調整期間）**の成績で行う。
- 早期打ち切りには、調整期間より前の部分の末尾を使う。
- 較正期間・評価期間・封印データは使わない。
- 既定値を変えるのは、候補の差が同じ条件でのシードによる揺れを上回るときだけ。

2026-09-17 以前に評価期間の成績で候補を比べた記録（JOURNAL）は、探索的な結果として扱う。

---

## 5. 傾向グループ（`tendency`）の採否

登録日時: 2026-09-20（この比較を実行する前）

### 問い

決まり手と進入の履歴から作った4特徴量（まくりで勝った割合、まくられて負けた割合、枠より内側に入る傾向、同じ枠でのその傾向）を加えると、1着確率の予測は良くなるか。いずれも対象レースより前のレースだけから作る。

### 実行方法（固定）

```bash
.venv/bin/boat-agent ablate --group tendency \
  --train 2023-01-01..2024-06-30 --calib 2024-07-08..2024-08-31 \
  --test 2024-09-08..2025-02-05 --coverage-year 2024 --data-root ./data-official
```

評価に使うのは**調整期間**（2024年9月〜2025年2月）で、成績確認に使ってきた2025年後半は使わない。LightGBM は既定値、シードは1・2・3で予測を平均する。

### 比較が有効である条件

1. 2024年のレースのうち、この4特徴量が付いている割合が98%以上
2. 学習期間のレース数が15,000以上
3. 両群とも ECE が0.01以下

### 判定

| 結果 | 判定 |
|---|---|
| Δ ≤ −0.001 かつ 95%区間の上限 < 0 | **採用**。最終モデルに入れる |
| それ以外 | **不採用**。特徴量は残すが、最終モデルからは外す |

基準を直前情報（0.003）より小さくしたのは、4列だけの追加で、狙いが「市場との差の一部」に限られるため。

### 参考として報告するもの（判断には使わない）

2025年後半での市場との差が、この4特徴量で縮むか（特にまくり決着のレース）。

## 4. 報告の約束

- 主要な数字は、シードを変えた複数回の結果か、その平均で出す。
- 場別・条件別など分けて見た結果は仮説として扱い、それだけで判断しない。

## 6. 単勝プールでの収益検証（草案・未開始）

**この節は提案であり、承認と収集開始までは有効にならない。** 開始前にこの節を確定さ
せ、確定した時点のコミットハッシュを記録する。背景は `docs/reports/market_edge_search.md`。

### 前提

2025年の探索では、オッズを一切見ずにモデルの本命を単勝で買うと回収率96.4%（市場の
1番人気は80.0%）だった。期待値で絞ると100%を超えるが、その選択には確定オッズ（締切
後にしか存在しない）を使っており、証拠にならない。

さらに2026-09-16..19の実測で、単勝の締切前オッズは確定オッズから大きくずれることが
分かった（締切3分前で|ずれ|中央38%、20倍以上の艇は確定で0.60倍）。したがってB・Cは
そのままでは落ちる可能性が高く、Dの補正が本命。3連単側は締切3分前で|ずれ|中央13%と
安定しており、この問題はない。

### 収集

- 締切3分前（許容: 1〜5分前）の単勝オッズと、確定単勝オッズを対で保存する。
- 開始日を決めたらその日以降のレースのみを対象とし、過去分は使わない。

### 判定に使う数字

締切前オッズで選び、確定オッズで払い戻した場合の、賭け1点あたり回収率。

| ルール | 内容 |
|---|---|
| A | モデルの本命を毎レース1点（オッズを選択に使わない） |
| B | `モデル確率 × 締切前オッズ > 1.0` の買い目すべて |
| C | `モデル確率 × 締切前オッズ > 1.2` の買い目すべて |
| D | `モデル確率 × 締切後オッズの予測値 > 1.0` の買い目すべて |

Dの「締切後オッズの予測値」は、締切前オッズと残り時間から確定オッズを予測する補正を
かけた値。補正式は収集開始前に確定させ、`docs/reports/market_edge_search.md` §6の
実測（単勝の20倍以上は締切3分前の0.60倍に収束する等）だけを根拠に作る。判定期間の
データで作り直さない。

- 1点100円の均等買い。賭け金の調整はしない。**ペーパートレードのみ。購入はしない。**
- モデルは収集開始時点のものを固定し、途中で学習し直さない。
- 中間結果は見てよいが、判定は事前に決めた終了条件まで行わない。

### 終了条件

Dの賭け数が39,000点に達した時点（約29,000レース、150.7レース/日で約195日）。または
開始から240日経過。早い方。

この規模が要るのは、狙う差が小さいと分かったため。`docs/reports/market_edge_search.md`
§7で、締切前オッズの実測誤差を注入すると§3の128〜156%は92〜106%に落ちる（区間は
いずれも100%をまたぐ）。判定すべきは「150%か」ではなく「100%か105%か」になった。

### 判定

| 結果 | 判定 |
|---|---|
| Dの回収率の95%区間の下限 > 100% | 実際に市場を上回る余地あり。買い方エージェントの設計に進む |
| 区間が100%をまたぐ | 判定保留。サンプルを増やすか、ここで打ち切る |
| 上限 < 100% | **否定**。単勝での収益化は追わない |

Aは対照として常に報告する。C単独では判定しない（閾値を後から選ぶことになるため）。

### 事前に認めない操作

- 場・レース番号・節などで後から絞り込んで再判定すること。
- 期待値の閾値を判定時に選び直すこと。
- モデルを収集期間中に更新すること。

## 7. 不一致ルール（単勝）

**この節は2026-09-20に、確認用データを取得する前に確定させた。** 発見の経緯は
`docs/reports/market_edge_search.md` §8。探索は2025-06-08..12-31の3,243レースで行った。

### ルール

各レースについて、モデルの勝率が最大の艇を t とする。締切3分前（許容1〜5分前）の
単勝オッズで1番人気となる艇を f とする。

- t ≠ f なら t の単勝を100円買う。
- t = f なら買わない。
- モデルは `config/final_model.json` を固定して使い、確認期間で学習し直さない。
- 賭け金は常に100円。調整しない。**ペーパートレードのみ。購入はしない。**

過去データで確認する場合、締切3分前の板は保存されていないため、実測した誤差分布
（`market_edge_search.md` §7の表。確定オッズ帯ごとの「表示÷確定」）で再現する。
再現は200抽選、区間はレース抽出の誤差と抽選間のばらつきを合算する。再現した1番人気が
確定の1番人気と一致する割合が70〜76%に入らない場合、再現が失敗しているとみなして
判定しない（実測値は72.9%）。

### 確認する期間

1. **2026-01-01..2026-08-31**（37,592レース）。モデルの学習にも較正にも使っていない。
   §2の最終試験で一度開封済みだが、この問いには使っていない。単勝の確定オッズを収集中。
2. 1で決着がつかない場合のみ、2026-09-21以降の前向き収集（締切前の実オッズを使うので
   再現が不要になる）。

### 判定

| 結果 | 判定 |
|---|---|
| 回収率の95%区間の下限 > 100% | **確認**。買い方エージェントの設計に進む |
| 区間が100%をまたぐ | **保留**。前向き収集に移る |
| 上限 < 100% | **否定**。単勝での収益化は追わない |

### 事前に認めない操作

- 区分をさらに細かく切って再判定すること（場別・レース番号別・モデル確率別など）。
- 「t ≠ f」を別の条件に置き換えること（例: 2番人気との比較、確率差の閾値）。
- 確認期間のデータで誤差分布を作り直すこと。
- 判定後に閾値やルールを変えて再試行すること。

### 想定される交絡

探索は17区分を見た中の1つなので、偶然当たりを引いた可能性がある。これが確認を別期間で
行う理由。また再現の前提（値動きが結果と無関係）は買い手に甘く、実際の前向き試験は
これより悪く出る見込み。

## 8. 前向きペーパー試験（実測の締切前オッズ）

**この節は2026-09-20に、対象レースが1件も存在しない時点で確定させた。** 背景は
`docs/reports/market_edge_search.md` §9。§7の確認（106.2% [103.3, 109.1]）は締切前の板を
再現して得たものであり、再現の甘さが結果を押し上げている可能性が残る。この試験は板を
再現せず実測する。

### 対象

開始日を2026-09-21とし、それ以降に締め切られたレースのみを対象とする。開始日より前の
レースは、締切前の板が保存されていても使わない。

各レースが対象になる条件:

1. 締切**1〜5分前**の単勝オッズが保存されている（複数あれば締切に最も近いもの）。
2. 確定単勝オッズが保存されている。
3. 6艇すべてに値がついている。

### ルール（§7と同一。板だけが再現から実測に変わる）

モデルの勝率が最大の艇を t、実測した締切前の板で1番人気の艇を f とする。

- t ≠ f なら t の単勝を100円買う。
- t = f なら買わない。
- モデルは `config/final_model.json` を固定。試験期間中に学習し直さない。特徴量は
  各レース時点の情報から毎日再計算する（重みは固定、材料は最新）。
- **ペーパートレードのみ。購入はしない。賭け金は常に100円で調整しない。**

### 終了条件

賭け数が10,000点に達した時点、または開始から240日経過。早い方。

10,000点の根拠: §7の実測で1点あたりのばらつきは1.56（賭け金を1とする単位）。10,000点で
95%区間の幅が±3ポイントになり、期待する上乗せ（6ポイント）と区別できる。対象になるのは
レースの約31%、1日あたり約47点なので、およそ7か月。

### 判定

| 結果 | 判定 |
|---|---|
| 回収率の95%区間の下限 > 100% | **確認**。買い方エージェントの設計に進む |
| 区間が100%をまたぐ | **保留**。§7の106%は再現の産物だった可能性が高いと記録して終える |
| 上限 < 100% | **否定**。単勝での収益化は追わない |

### 途中で見てよいもの／見てはいけないもの

見てよい（データの健康状態）: 対象レース数、賭け数、締切までの分の分布、カバレッジ、
取得失敗。

**見てはいけない: 回収率、的中率、損益。** 終了条件を満たすまで計算しない。これは
コードで強制する（`boat-agent disagreement-forward` は終了条件を満たすまで回収率を出さず、
`--unblind` で強制した場合はその事実を `logs/forward_unblinded.log` に記録する）。

### あわせて記録するもの（判定には使わない）

- 買った艇の「締切前オッズ ÷ 確定オッズ」の分布。§7の再現がどれだけ甘かったかが分かる。
- 同じレースを再現ベース（§7の手法）で判定した場合の結果。両者の差が再現バイアスの実測値。
- 買わなかったレース（t = f）の回収率。

### 事前に認めない操作

- 区分を切って再判定すること（場別・レース番号別・モデル確率別など）。
- 「t ≠ f」を別の条件に置き換えること。
- 締切までの許容範囲（1〜5分）を判定後に変えること。
- モデルを試験期間中に更新すること。
- 終了条件を満たす前に判定し、その結果を見て続行を決めること。

### 想定される限界（この試験でも消えないもの）

執行コストを含まない。実際に薄い単勝プールへ1日47点入れれば価格は動き、モデルの買い目は
定義上「人気のない側」なので影響は大きい方に出る。この試験が確認で終わっても、次は
執行の検証であって購入ではない。

### §8 の修正（2026-09-20、試験開始前・対象レース0件の時点）

登録内容を自分で見直し、実データで前提を確認したうえで以下を確定させた。いずれも
開始前の修正であり、結果を見てからの変更ではない。

**1. 1番人気が同値で並ぶ場合（実測1.64%）**

最低オッズの艇が複数いるとき、そのすべてを1番人気として扱う。モデルの本命がその中に
含まれるなら買わない。艇番の若い順などで勝手に決めない。

**2. 除外するレース**

※ この項目は2026-09-20夜の再レビューで誤りが見つかり、下の「最終確定版」で置き換えた。
当初「いずれも締切前に判別でき、結果とは無関係」と書いたが、**不成立と同着は締切前には
分からない**。予測時点の除外と結果確定後の除外を混ぜていた。

**3. 必要な賭け数を分散から一度だけ更新する（内部パイロット）**

10,000点という目標は§7で測ったばらつき（1点あたり1.56）に基づく。実際のばらつきが
違えば、短すぎて結論が出ないか、答えが出た後も数か月続けることになる。

賭け数が2,000点に達した時点で**払戻のばらつき（標準偏差）だけ**を一度測り、必要点数を
`n = (1.96 × SD ÷ 0.03)²` に更新する。下限10,000点、上限20,000点。

このとき**回収率・的中率・損益は計算しない**。コード上も分散だけを返す関数を通す
(`payout_spread`)。更新は一度きりで、その後は変えない。

**4. 取得率の報告義務**

締切1〜5分前の板が取れた割合を、場別・時間帯別に報告する。対象レースが全レースの60%を
下回った場合、結果は「取得できた部分集合についての結論」と明記し、全レースへの一般化は
主張しない。

実測（2026-09-16..19）では、締切に最も近いスナップショットが1〜5分に入るのは78.7%
だった。開始前の対策として、ラストコール収集の取得対象を単勝・3連単に絞り（直前情報は
バックフィルの30日ローリングへ移動）、同じ時間内により多くのレースを拾えるようにした。

**5. モデルの陳腐化について**

モデルは2025-09-30までの学習で固定する。判定時点では約18か月前の重みになる。

※ 当初ここに「陳腐化は結果を悪くする方向なので保守的な誤り」と書いたが、断言できない。
制度・選手構成・市場の変化で偶然良くなる可能性もある。**固定の目的は、試験開始後の
適応による研究者自由度を排除することであり、陳腐化による性能変化の向きは事前に仮定
しない。** 学習し直した版は併記しない（併記すれば良い方を選びたくなる）。

**6. この試験では測れないもの**

プールの規模（売上）はオッズページに載っておらず、現在のデータでは執行の影響を試算
できない。1日47点を薄い単勝プールに入れれば価格は動くが、その大きさはこの試験では
分からない。執行の検証は別の関門であり、その前提として売上データの収集経路を先に
用意する必要がある。


### §8 最終確定版（2026-09-20 夜、対象レース0件の時点で凍結）

再レビューで3点の誤り・不足が見つかったため、以下を最終版として固定する。
**最初の対象レースが発生した後は一切変更しない。**

```text
Primary rule
  締切1〜5分前の実測単勝板において
    市場1番人気が一意
    AND モデル本命 != 市場1番人気
  → モデル本命を100円買ったと仮定

Tie
  市場1番人気が複数 → SKIP（実測1.64%、約61レースに1回）

Stake
  常に100円

Model
  config/final_model.json、シード1,2,3の平均。試験期間中固定
  （目的は研究者自由度の排除。陳腐化の向きは仮定しない）

Eligibility exclusion（予測時点で判別できるもの）
  締切前スナップショットの時点で単勝板が6艇そろっていない

Evaluation exclusion（結果確定後、事前に定めた機械的な評価不能条件のみ）
  公式結果ファイルに単勝払戻の記載がないレース（不成立・特払い等）
  ※ 同着は除外しない。下記のとおり実払戻で評価する

Payout
  公式結果ファイルの単勝払戻金をそのまま使う
  （同着は両艇に払戻が記載されるので、買った艇が含まれていればその金額）
  オッズ×100では同着も不成立も表現できないため、価格ではなく金額で評価する

Interim
  2,000 bets時点で払戻分布のSDのみ取得
  ROI・累積損益・勝敗は非表示（payout_spread が分散しか返さない）

Target N
  (1.96 × SD / 0.03)^2 を 10,000〜20,000 にclamp
  ※ これは試験期間を決めるための近似であり、判定には使わない

Primary endpoint
  総払戻 / 総仮想購入額

Primary CI
  day-cluster bootstrap
  1日を1クラスタとして日単位で復元抽出 → その標本の 総払戻/総購入額 を計算
  → 10,000回 → 2.5%点 / 97.5%点
  seed 20260921 で固定（再実行しても同じ区間が出る）
  ※ 単勝払戻は0円が大半で右に長い裾を持ち、同日・同場のレースには依存がある。
    正規近似は実際より狭い区間を出すため、主解析には使わない

Decision
  95% CI lower > 100%  → confirm
  95% CI includes 100% → inconclusive
  95% CI upper  < 100% → reject

Coverage
  全対象候補に対する1〜5分前板の取得率 + 場別 + 時間帯別
  60%を下回れば「取得できた部分集合についての結論」と明記

No changes after first eligible race
```

### §8 運用ルール: 何を直してよいか（2026-09-20 夜に確定）

7か月は、2か月目に何を触ったか忘れるには十分な長さがある。原則は「装置は直す、実験は
いじらない」。文章だけでは守れないので、判定を左右するファイルを指紋で固定し、変更が
あればテストが落ちるようにした。

```text
DATA-HEALTH（試験中も修正してよい）
  取得失敗の修正
  parserの修正
  タイムゾーンの修正
  欠損監視の修正
  ダッシュボード・ログ

ANALYSIS-AFFECTING（試験中は禁止）
  予測モデルの変更（特徴量の追加を含む）
  eligibilityの変更
  payout計算の変更
  判定ルール・区間算出方法の変更
  別モデルの併記
```

分類は**意図ではなく結果**で決める。「parserの修正」でも、それが対象レースの集合を
変えるなら ANALYSIS-AFFECTING。だからラベルを信用せず、ファイルの中身の指紋を見る。

固定対象は `config/trial_lock.json` に記録した10ファイル（`config/final_model.json`、
`disagreement.py`、`disagreement_check.py`、`experiment.py`、`final_eval.py`、
`features/registry.py`、`features/builder.py`、`models/lgbm.py`、`models/calibration.py`、
`models/market.py`）。収集・parser・正規化・監視は**意図的に対象外**で、自由に直せる。

- 確認: `boat-agent trial-lock`（変更があれば終了コード1）
- テスト: `tests/test_trial_lock.py` が毎回検査する
- 前向き試験のレポートにも毎回、装置が凍結時と一致しているかを印字する
- 変更履歴: `logs/trial_changes.log`

試験終了後に凍結し直す場合のみ `boat-agent trial-lock --freeze` を使う。
